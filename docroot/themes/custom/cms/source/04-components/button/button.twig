{% if is_button %}
  <button class="ds-c-button {{ modifier }}"{% if is_disabled %} disabled{% endif %}>{{ text }}</button>
{% else %}
  <a href="{{ url }}" class="ds-c-button {{ modifier }}">{{ text }}</a>
{% endif %}
